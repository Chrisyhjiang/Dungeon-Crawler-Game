#include "enemy.h"
#include "enemyFactory.h"

Enemy* EnemyFactory::createEnemy(char en){
    Enemy* en

    switch(en){
        case Enemy::ENEMY_HUMAN:
            en = new 
        
            break;
        case: ''

    }

}