#include "dwarf.h"

Dwarf::Dwarf(int hp, int atk, int def, string race, char symbol, string name) : Enemy(hp, atk, def, race, symbol, name){}
Dwarf::~Dwarf(){
    //Todo:
}

int calculateDamageToPlayer(){
    //Todo:
}